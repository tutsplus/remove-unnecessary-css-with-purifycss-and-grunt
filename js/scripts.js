$(document).ready(function() {
    var className = "navbar-",
        secondaryClassName = ['navbar', 'inverse'].join('-');

    // $(".navbar").addClass("navbar-inverse");
    // $(".navbar").addClass(className + "inverse");
    $(".navbar").addClass(secondaryClassName);
});
